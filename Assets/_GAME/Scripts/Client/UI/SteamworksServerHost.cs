using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Creates a server and then connects the host to it,
/// This should tell Steam that you've made a server and then 
/// update the steam master server by adding your server to it
/// </summary>
public class SteamworksServerHost : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
