using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// A simple Singleton wrapper around the Steamworks library
/// This should be your primary access to and area to call
/// all of your Steamworks code from.
/// </summary>
public class SteamworksManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
